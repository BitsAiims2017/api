const config = {
    host : 'localhost',
    port : 2000,
    db: {
        host: 'localhost',
        port: '27017',
        name: 'dbname'
    },
    token: {
        secret: 'hidden_yapper'
    }
};

module.exports = config;
